{
  "buildCommand": "rm -rf .next && npx prisma generate && npx prisma db push --skip-generate && next build",
  "devCommand": "npm run dev",
  "installCommand": "rm -rf node_modules/.prisma && npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}

